# 00-tools AI作業ルール

このリポジトリは、ユーザーがChatGPTに小さなWindows用ツールを作らせ、GitHubからダウンロードして使うための保管庫です。

## 最重要ルール

Windows用の .cmd / .bat / PowerShell を作成・修正する場合は、完成後に必ず全文を再確認してからユーザーへ渡すこと。

特に次を必須とする。

- 実行失敗時にウィンドウを自動で閉じない
- エラー内容を画面に表示する
- エラー内容をログにも保存する
- 呼び出す外部機能やコマンドが存在するか事前確認する
- 古い処理、重複コード、途中で残ったコード片が混ざっていないか全文確認する
- 一時ファイルを使う場合、失敗時に原因調査できるようにする
- ダウンロード対象は可能な限り1ファイルにまとめる
- 初回設定はPC側へ自動保存し、後から変更できるようにする
- ユーザーに手作業でコードファイルを作らせない
- Tampermonkeyは、他に現実的な方法がない場合のみ使う
- ショートカットキー前提の操作は避け、クリック中心にする

## ChatGPT複数表示.cmd

目的:
- ChatGPTの指定した2～4個のチャットを別ウィンドウで開く
- 各ウィンドウを縦長の列として横並びに配置する
- 保存済みチャットを後から入れ替えられる
- ユーザーがダウンロードするファイルは1個だけにする

修正時は、既存機能を壊さず、起動失敗時に必ずエラーが残ることを確認する。

## 引き継ぎ方針

「前のチャットで決めたから覚えているはず」と考えないこと。
この AGENTS.md と README.md を作業前に確認し、ここに書かれた内容を優先すること。
